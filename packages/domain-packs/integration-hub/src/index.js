import { detect, refine } from "./refinement.js";

export const id = "integration-hub";
export const name = "Integration Hub";
export { detect, refine };

export default { id, name, detect, refine };
